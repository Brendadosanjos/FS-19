// METODOS ARRAY

// SLICE: Recorte para impressão de um array (indice inicial e o indice final -1)
const number = [0, 2, 5, 6, 7, 8]
console.log(number.slice(1,3));


//FROM:
Array.from(({length: 10})).map(()=>console.log(Array);)